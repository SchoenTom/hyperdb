# Coverage & Distribution Report

> **AUTO-GENERATED — not yet produced.** Overwritten by `python cli.py audit --coverage` after
> the build. Placeholder only; no coverage is claimed until it is measured.

Planned contents (transparency for "good, legitimate coverage"):

- **Cross-sectional breadth:** number of common-equity entities per month, per major market,
  over time (active vs delisted split).
- **Temporal completeness:** per-asset trading-day completeness vs the real exchange calendar;
  histogram of completeness.
- **Survivorship:** count and share of delisted entities by era and exchange.
- **Identity:** entities, multi-listing entities, reuse-splits, low-confidence links
  (see ENTITY_RESOLUTION.md).
- **Return distributions:** by region/decade (mean, sd, skew, kurtosis) vs published benchmarks.
- **Gaps & screens:** rows flagged by each screen; sentinel/gap concentration by exchange/era.
- **Currency/FX:** coverage by currency and period; pre-2000 gaps surfaced.

Figures will be written to `reports/figures/` by `generate_figures.py`.

Vintage: *(none built yet)*
